
public class OperatorGameControl {
/*
	Model model;
	View view;

	public void actionPerformed(java.awt.event.ActionEvent e){
	
		if(e.getSource() == tileInHandButton)
		{
			//Get a reference to tile and tell model which tile was selected.
		}
		else if (e.getSource() == tileOnBoardButton )
		{
			//Get a reference to tile and tell model which tile was selected.
		}
		//Do this for every button in view.
	}

	public void addModel(Model m){
		this.model = m;
	}

	public void addView(View v){
		this.view = v;
	}

	public void initModel(int x){
		model.setValue(x);
	} 
*/
}
